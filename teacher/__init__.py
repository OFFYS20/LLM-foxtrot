"""Teacher — a small command-line program for teaching a language model.

Where AI Studio is a workbench with every dial exposed, Teacher is the short
path: point it at text, and it builds a tokenizer, a model and a training run
for you. Each model lives in its own folder, and that folder is exactly what
the Bench web page loads.
"""

__version__ = "0.1.0"
